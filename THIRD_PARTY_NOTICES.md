# Third-party notices

The code and art authored for DEADWIRE are licensed separately in `LICENSE` and
`assets/LICENSE.md`. Dependencies retain their upstream licenses:

- Three.js: MIT; including its glTF loader, geometry utilities, and RoomEnvironment.
- Vite, TypeScript, Vitest, Playwright, Prettier, and their dependencies: see the
  license files distributed in `node_modules` and the exact dependency tree in
  `package-lock.json`.
- Blender is a separately installed GPL application; it is not redistributed here.

The game uses system fonts and original procedural texture pixels. It does not
include downloaded models, paid-generation outputs, franchise assets, or commercial
music. Asset manifests retain generation provenance and review notes. Weapon names
identify the prototype's gameplay profiles and do not imply manufacturer endorsement.
